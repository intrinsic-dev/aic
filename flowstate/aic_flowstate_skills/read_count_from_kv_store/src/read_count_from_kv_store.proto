syntax = "proto3";

package ai.flowstate;

message ReadCountFromKVStoreParams {
  // Key name in the Key-Value store
  string key = 1;
}

message ReadCountFromKVStoreResult {
  // Integer value / counter read from the KV store
  int64 count = 1;

  bool success = 2;
  string message = 3;
}
