syntax = "proto3";

package ai.flowstate;

message WriteToKVStoreParams {
  // Key name in the Key-Value store
  string key = 1;

  // Integer value / counter to write to the KV store
  int64 count = 2;
}

message WriteToKVStoreResult {
  bool success = 1;
  string message = 2;
}
