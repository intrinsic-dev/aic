syntax = "proto3";

package ai.flowstate;

message StartRecordingSkillParams {
  // Optional name tag for the bag file recording.
  string name = 1;
}

message StartRecordingSkillResult {
  // Path to the created bag recording.
  string bag_path = 1;
}
