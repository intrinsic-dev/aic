> [!IMPORTANT]
> Note that this package should never be published.

This is a dummy package to workaround ros's lack of support for pypi packages.

This package contains a custom build script that creates a venv and installs the required pypi packages on it.
It exports `aic_venv_python3` to run python scripts under the venv.
